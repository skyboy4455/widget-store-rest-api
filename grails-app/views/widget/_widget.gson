import com.acme.widgets.products.Widget

model{
    Widget widget
}

json{
        sku widget.sku
        type widget.widgetType.name
        finish widget.widgetFinish.name
        size widget.widgetSize.name
}